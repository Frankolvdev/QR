import { redirect } from "next/navigation"; import bcrypt from "bcryptjs"; import { db } from "@/lib/db"; import { createSession,currentUser } from "@/lib/auth";
export default async function Login(){if(await currentUser()) redirect("/");
 async function login(fd:FormData){"use server";const email=String(fd.get("email")||"").trim().toLowerCase();const password=String(fd.get("password")||"");const u=await db.user.findUnique({where:{email}});if(!u||!u.active||!(await bcrypt.compare(password,u.passwordHash))) redirect("/login?error=1");await createSession(u.id);redirect("/");}
 return <main className="wrap" style={{maxWidth:460,paddingTop:80}}><div className="card"><h1>QR System</h1><p className="muted">Administración de tarjetas QR dinámicas + NFC.</p><form action={login}><label>Correo</label><input className="input" name="email" type="email" required/><label>Contraseña</label><input className="input" name="password" type="password" required/><button className="btn">Iniciar sesión</button></form></div></main>}