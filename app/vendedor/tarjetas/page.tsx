import {currentUser} from '@/lib/auth';import {redirect} from 'next/navigation';import Shell from '@/components/Shell';import {db} from '@/lib/db';
export default async function Cards(){const u=await currentUser();if(!u)redirect('/login');const cards=await db.card.findMany({where:{sellerId:u.id},orderBy:{updatedAt:'desc'},take:50});return <Shell title="Área de vendedor" user={u} active="cards"><div className="pageHead"><div><span className="eyebrow">MIS CLIENTES</span><h1>Mis tarjetas</h1><p>Consulta las tarjetas que has registrado.</p></div></div><section className="panel">{cards.length===0?<div className="emptyState"><h3>Aún no tienes tarjetas</h3><p>Las tarjetas que registres aparecerán aquí.</p></div>:<div style={{overflowX:'auto'}}><table><thead><tr><th>Código</th><th>Negocio</th><th>Estado</th></tr></thead><tbody>{cards.map(c=><tr key={c.id}><td>{c.code}</td><td>{c.businessName||'—'}</td><td>{c.status}</td></tr>)}</tbody></table></div>}</section></Shell>}
