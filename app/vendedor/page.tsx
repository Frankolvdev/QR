import {currentUser} from '@/lib/auth';import {redirect} from 'next/navigation';import Shell from '@/components/Shell';
export default async function Seller(){const u=await currentUser();if(!u)redirect('/login');return <Shell title="Área de vendedor" user={u} active="qr"><div className="pageHead"><div><span className="eyebrow">ACTIVACIÓN</span><h1>Registrar nueva tarjeta</h1><p>Vincula una tarjeta física con el negocio de tu cliente.</p></div></div><div className="registerLayout"><section className="panel registerPanel"><div className="steps"><div className="step on"><b>1</b><span>Código</span></div><i/><div className="step"><b>2</b><span>Información</span></div><i/><div className="step"><b>3</b><span>NFC</span></div></div><h2>Identifica la tarjeta</h2><p className="muted">Escanea el QR impreso o escribe el código que aparece debajo.</p><label>Código de tarjeta</label><div className="codeRow"><input className="input" placeholder="Ej. QR-7F3K9D"/><button className="scanBtn">▦ Escanear</button></div><button className="primaryBtn full">Continuar →</button></section><aside className="helpCard"><span className="helpBadge">PASO 1 DE 3</span><h2>¿Dónde encuentro el código?</h2><p>Cada tarjeta tiene un código único impreso debajo del QR.</p><div className="demoCard"><span>QR CONNECT</span><b>▦</b><small>QR-7F3K9D</small></div><p className="tip">💡 Si la cámara no está disponible, siempre puedes escribirlo manualmente.</p></aside></div></Shell>}
