import {currentUser} from '@/lib/auth';import {redirect} from 'next/navigation';import Shell from '@/components/Shell';import {Icon} from '@/components/Icons';
export default async function Page(){const u=await currentUser();if(!u)redirect('/login');if(u.role!=='ADMIN')redirect('/vendedor');return <Shell title="Vendedores" user={u} active="users"><div className="pageHead"><div><span className="eyebrow">ADMINISTRACIÓN</span><h1>Vendedores</h1><p>Este módulo ya forma parte de la nueva navegación y se completará en la siguiente fase funcional.</p></div></div><section className="panel moduleComing"><span><Icon name="users"/></span><h2>Vendedores</h2><p>La base visual y responsive está lista.</p></section></Shell>}
