import {currentUser} from '@/lib/auth';import {redirect} from 'next/navigation';import Shell from '@/components/Shell';import {Icon} from '@/components/Icons';
export default async function Page(){const u=await currentUser();if(!u)redirect('/login');if(u.role!=='ADMIN')redirect('/vendedor');return <Shell title="Tarjetas" user={u} active="cards"><div className="pageHead"><div><span className="eyebrow">ADMINISTRACIÓN</span><h1>Tarjetas</h1><p>Este módulo ya forma parte de la nueva navegación y se completará en la siguiente fase funcional.</p></div></div><section className="panel moduleComing"><span><Icon name="cards"/></span><h2>Tarjetas</h2><p>La base visual y responsive está lista.</p></section></Shell>}
