import {currentUser} from '@/lib/auth';import {redirect} from 'next/navigation';import Shell from '@/components/Shell';import {db} from '@/lib/db';
export default async function Page(){const u=await currentUser();if(!u)redirect('/login');if(u.role!=='ADMIN')redirect('/vendedor');const [total,active,available,disabled,sellers,audits]=await Promise.all([db.card.count(),db.card.count({where:{status:'ACTIVE'}}),db.card.count({where:{status:'AVAILABLE'}}),db.card.count({where:{status:'DISABLED'}}),db.user.count({where:{role:'SELLER',active:true}}),db.auditLog.findMany({include:{user:true},orderBy:{createdAt:'desc'},take:30})]);return <Shell title="Reportes" user={u} active="chart"><div className="pageHead"><div><span className="eyebrow">MÉTRICAS</span><h1>Reportes</h1><p>Vista rápida del inventario y la actividad reciente.</p></div></div><div className="statsGrid"><div className="stat"><div><strong>{total}</strong><small>Total</small></div></div><div className="stat green"><div><strong>{active}</strong><small>Activas</small></div></div><div className="stat blue"><div><strong>{available}</strong><small>Disponibles</small></div></div><div className="stat amber"><div><strong>{sellers}</strong><small>Vendedores activos · {disabled} deshabilitadas</small></div></div></div><section className="panel"><div className="panelHead"><div><h2>Auditoría reciente</h2><p>Últimas acciones registradas</p></div></div><div className="tableWrap"><table><thead><tr><th>Fecha</th><th>Usuario</th><th>Acción</th><th>Detalle</th></tr></thead><tbody>{audits.length?audits.map(a=><tr key={a.id}><td>{a.createdAt.toLocaleString('es-MX')}</td><td>{a.user?.name||'Sistema'}</td><td>{a.action}</td><td>{a.detail||a.entity}</td></tr>):<tr><td colSpan={4} className="muted">Sin actividad todavía.</td></tr>}</tbody></table></div></section></Shell>}
